# Full environment for running the dashboard, demo, and Docker image.
# The pip package itself has a leaner core — see setup.py install_requires.
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
click>=8.1.0
prometheus-client>=0.18.0
sqlalchemy>=2.0.0
rich>=13.0.0
httpx>=0.25.0
