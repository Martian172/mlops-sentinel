global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "mlops-sentinel"
    metrics_path: /metrics
    static_configs:
      # "sentinel" is the compose service name (containers resolve each
      # other by service name on the compose network)
      - targets: ["sentinel:8080"]
